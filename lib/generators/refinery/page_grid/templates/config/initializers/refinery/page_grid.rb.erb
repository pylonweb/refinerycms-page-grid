Refinery::PageGrid.configure do |config|
	# Configure which margin options that should be available
  # config.margin_options = <%= Refinery::PageGrid.margin_options.inspect %>

  # Configure which width options that should be available
  # config.width_options = <%= Refinery::PageGrid.width_options.inspect %>

  # Configure which class to apply to the section for the margin. '#' will be replaced by the margin.
  # config.margin_class = <%= Refinery::PageGrid.margin_class.inspect %>

  # Configure which class to apply to the section for the width. '#' will be replaced by the width.
  # config.width_class = <%= Refinery::PageGrid.width_class.inspect %>
end